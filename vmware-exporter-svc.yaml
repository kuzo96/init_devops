apiVersion: v1
kind: Service
metadata:
  name: vmware-exporter
  namespace: default
  labels:
    app: vmware-exporter
spec:
  selector:
    app: vmware-exporter
  ports:
    - name: http
      port: 9272
      targetPort: 9272
